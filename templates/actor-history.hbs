<div class="tab historia" data-group="primary" data-tab="historia">
    <div class="hogwart-history-container house-{{system.dom}}">
        <h3 class="history-title">📖 {{localize "HOGWART.History.Tytul"}}</h3>

        <div class="form-group">
            <textarea name="system.biografia"
                      rows="12"
                      placeholder="{{localize "HOGWART.History.op"}}"
                      class="history-textarea">{{system.biografia}}</textarea>
        </div>
    </div>
</div>